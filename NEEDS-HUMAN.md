# NEEDS-HUMAN (the only file you must watch)

Format: `- [<UTCstamp>] [<role>] <action> - <how to respond>`

- [20260628] [MAIN] Publish rein-engine 0.4.0 to PyPI - on github.com/SametAtas/rein
  go to Releases -> Draft a new release -> pick existing tag v0.4.0 -> title
  "rein-engine 0.4.0" -> notes from the CHANGELOG 0.4.0 block -> Publish; then
  approve the `pypi` environment when publish.yml prompts.
- [20260628] [MAIN] Confirm CRITICAL_PATH item 2 (pin the Keystone arm to 6067d73) -
  reply "pin go" and MAIN runs it (it alters the running experiment).
